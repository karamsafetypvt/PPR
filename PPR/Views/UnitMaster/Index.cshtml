@using PPRModel.Model
@model UnitMasterModel
@{
    ViewBag.Title = "Unit Master";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int i = 0;
}
<style>
    .tblrespoonsive {
        overflow-x: auto;
    }
    td.textcenter {
        text-align: center
    }
   /* .sweat-alert-inner .sa-icon .sa-warning .col-xs-3 .pulseWarning {
        left: 10px !important;
    }*/
    .sweet-alert .sa-icon.sa-warning {
        border-color: #F8BB86;
        left: 10px;
    }
</style>
<link href="~/Content/css/sweetalert.css" rel="stylesheet" />
<div id="content">
    <div id="content-header">
        <div id="breadcrumb"> <a href="~/Dashboard/Index" class="tip-bottom"><i class="icon-home"></i> Home</a> <a href="" class="current">Calibartion Unit Master</a> </div>
        <h1>Calibartion Unit Master</h1>
    </div>
    <div class="container-fluid">
        <div class="row-fluid">
            <div class="span12">
                <div>
                    <a href="@Url.Action("Create", "UnitMaster")" class="btn btn-primary">
                        <span>
                            <i class="fa fa-plus"></i>
                            <span>New</span>
                        </span>
                    </a>
                </div>
                <div class="widget-box">
                    <div class="widget-title">
                        <span class="icon"><i class="icon-th"></i></span>
                        <h5>Calibartion Unit Master</h5>
                    </div>

                    <div class="widget-content nopadding tblrespoonsive">
                        <table class="table table-bordered data-table">
                            <thead>
                                <tr>
                                    <th>Sr. No.</th>
                                    <th style="display:none">ID</th>
                                    <th>Name</th>
                                    <th>Description</th>
                                    <th>Status</th>
                                    @if (Convert.ToInt16(Session["PPR_LoggedUserType"]) == 1)
                                    {
                                        <th>Action</th>
                                    }
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var data in Model._UnitMasterModelList)
                                {
                                    <tr class="gradeX">
                                        <td class="textcenter">@(++i)</td>
                                        <td style="display:none">@data.Id</td>
                                        <td class="textcenter">
                                            @data.Name
                                        </td>
                                        <td class="textcenter">@data.Discription</td>
                                        @if (@data.Status == "Active")
                                        {
                                            <td style="text-align: center;"><span class="badge badge-success">@data.Status</span></td>
                                        }
                                        else
                                        {
                                            <td style="text-align: center;"><span class="badge badge-danger">@data.Status</span></td>
                                        }
                                        @if (Convert.ToInt16(Session["PPR_LoggedUserType"]) == 1)
                                        {

                                            <td class="textcenter" style="width: 11%;">
                                                <a href="@Url.Action("Edit", "UnitMaster", new { CategoryID = @data.Id })" class="btn btn-primary" title="Edit"><span class="fa fa-pencil"></span></a>
                                            </td>
                                        }
                                    </tr>
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<script src="~/Content/js/sweetalert-dev.js"></script>


